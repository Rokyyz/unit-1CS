def name()<-argument
return <- output
def square(x:int) <- type hint
def largerInteger(A:int, B:int) -> :int:
  output 'A'
  if A<B:
    output = 'B'
  return output

r = largerInteger(2012,15)
print(r)

->
inputs -> name -> outputs
calling the f(x) = x^2
function -> f(x=3) = 9

ate:bol
   :str
   :list
   :dict
